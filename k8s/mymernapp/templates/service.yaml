apiVersion: v1
kind: Service
metadata:
  name: {{ include "mymernapp.fullname" . }}
  labels:
    {{- include "mymernapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mymernapp.selectorLabels" . | nindent 4 }}
