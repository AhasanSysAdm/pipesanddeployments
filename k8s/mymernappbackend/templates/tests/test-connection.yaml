apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mymernappbackend.fullname" . }}-test-connection"
  labels:
    {{- include "mymernappbackend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mymernappbackend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
